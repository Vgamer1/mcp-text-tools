{
  "name": "mcp-text-tools",
  "version": "1.0.0",
  "description": "Paid MCP server: diff_text, count_tokens, chunk_text — hosted on Cloudflare Workers with Stripe billing",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "@stripe/agent-toolkit":    "^0.1.0",
    "agents":                   "^0.0.95",
    "diff":                     "^7.0.0",
    "tokenx": "^1.3.0",
    "zod":                      "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript":                "^5.4.5",
    "wrangler":                  "^4.0.0"
  }
}
