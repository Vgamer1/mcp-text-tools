{
  "name": "mcp-text-tools",
  "version": "1.0.0",
  "description": "MCP server: diff_text, chunk_text — hosted on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "diff":                     "^8.0.4",
    "tokenx":                   "^1.3.0",
    "zod":                      "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript":                "^5.4.5",
    "wrangler":                  "^4.0.0"
  }
}